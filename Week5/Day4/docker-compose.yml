services:

  backend:
    image: node:18
    working_dir: /app
    volumes:
      - ./backend:/app
    command: node index.js

  nginx:
    image: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs